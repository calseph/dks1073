#Question 5 – Access Control System (Nested if-else)
#Scenario:
#If user is staff:
    #If access level >= 5 → Access granted
    #Else → Access denied: insufficient level
#If not staff → Access denied: staff only

# Ask user for input
is_staff = input("Are you staff? (yes/no): ")

if is_staff == "yes":
    access_level = int(input("Enter your access level (number): "))
    if access_level >= 5:
        print("Access granted.")
    else:
        print("Access denied: Insufficient level.")
else:
    print("Access denied: staff only")
